# ProsotaPMO — Project State

**Last updated:** 2026-07-04
**Updated by:** Claude (Anthropic), session 20
**Status:** Production build well underway. Risk Register, ICD Tracker, Cost Plan, and Scheduling (incl. Resources) are built and click-tested end-to-end by the founder. Controls Dashboard, File Manager, Period Manager (real one), Export Center, and all 4 AI capabilities are not yet started.

> **Instructions for whoever reads this next (human or Claude):** This file is the single source of truth for where ProsotaPMO stands. Read it fully before writing any code. Update it at the end of every working session — what changed, what's next, what's blocked. Treat stale entries as a bug in the project, not just the document. **For the full plain-English blow-by-blow of every session, see `docs/LEARNING_LOG.md` — this file's Section 9 is a condensed technical index into that, not a replacement for it.**

---

## 1. What This Project Is

ProsotaPMO is a browser-native, AI-powered, sector-agnostic project management platform for organisations running structured PMOs — schedule, cost, risk, and change control in one integrated tool. Built by founder Maro Sota (Louis Oghenemaro Sota, Senior Planner / 4D Project Controls Specialist, Prosota Consulting Ltd) in direct collaboration with Claude as the engineering partner.

Full business rationale: see `docs/business-case.docx` (not yet copied into repo — pull from prior session output).
Full prototype scope: see `docs/project-scope.docx` (not yet copied into repo — pull from prior session output).

## 2. Current State (As of This Handoff)

The original prototype (`docs/prototype/prosota-pmo.html`, ~6,400 lines) remains the UX reference spec, but production has moved well past it in places (Scheduling in particular is now genuinely more capable than the prototype — real CPM, hour-precision calendars, Resources). Treat the prototype as a starting reference, not the ceiling.

**Built, tested (322 backend tests, `tsc --noEmit` clean), and click-confirmed by the founder in a real browser:**

- **Auth & shell** — Auth0 SSO login, auto-provisioning of org/user on first login, sidebar shell with all 8 module slots.
- **Project Selector** — create/switch projects; safe multi-select Archive (reversible) and permanent Delete (cascades cleanly across every dependent table — periods, activities, risks, cost elements, ICD items, resources, calendars, baselines, and all their children).
- **Risk Register** — full PMBOK-aligned rebuild: cause/effect/rationale statement, threat/opportunity split with correct asymmetric EMV sign convention, inherent + residual 5×5 heat-matrix, mitigation actions sub-list, configurable probability/impact criteria & thresholds, 3-point (min/most-likely/max) cost & schedule estimates, editable reassessment log, full toolbar (search/filter/group/CSV export/print).
- **ICD Tracker** — Issues/Changes/Decisions in one tracker, Rita Mulcahy Issue Log fields, real Integrated Change Control fields (change type, CCB decision, rejection reason, contract reference, cost/EOT claims, quality impact), action items sub-list, real per-user comments, KPI strip, same toolbar as Risk.
- **Cost Plan** — fixed/percentage cost elements, real computed EVM (CV/CPI/EAC/ETC — SPI now real, see Scheduling below), configurable variance-band thresholds, rate-card breakdowns, open commitments, £/m² and £/space summary, same toolbar/reassessment pattern.
- **Scheduling** — the largest module: MS-Project-style WBS (indent/outdent, auto-promote to summary), full dependency engine (FS/SS/FF/SF + lag/lead) with cycle detection, multi-calendar support with per-activity override, **a genuine hour-precision critical-path engine** (forward/backward pass, calendar-aware, total/free float, in-progress activities correctly protected from reschedule), named/saved baselines, saved visual Layouts + shared editable print letterhead, DCMA 14-point schedule quality checks (12 of 14 implemented), reschedule tool with an exact-date picker, full Earned Value column set (AC/PV/EV/CV/SV/CPI/SPI/BAC/EAC/ETC) synced one-way from Activity → Cost Plan, zoomable Gantt (Day/Week/Month/Quarter/Year) with WBS bracket rows, collapse/expand, bulk row actions (multi-select indent/outdent/duplicate/assign-common), and a proper two-page-fitting print view.
- **Resources module** (`👷 Resources`, sits alongside Scheduling's Calendar) — Labour/Equipment/Material/Subcontractor pool; Labour & Equipment cost via day-rate × utilisation% (follows duration automatically), Materials via qty × rate, Subcontractors as lump sum; resource assignments drive a linked Cost Plan line automatically unless a human deliberately overrides it (one-way "unlink," never silently reconnected).

**Not yet started:** Controls Dashboard (all 3 tabs), File Manager, real Period Manager (currently a stopgap auto-created "Period 1"), Export Center, all 4 AI capabilities (co-pilot, baseline narrative, Setup Assistant, Generative Visual Assistant), real Import (XER/MPP/Asta PP/XML file parsing — deliberately deferred every time it's come up, to avoid building a fake import UI), AWS production infrastructure.

## 3. Immediate Next Steps

In priority order:

1. [x] Initialise repo, copy reference docs in, FastAPI scaffold, Auth0, CI/CD (all Sprint 1 tasks bar AWS — see [[project_prosotapmo_sprint1]])
2. [x] Risk Register, ICD Tracker, Cost Plan, Scheduling, Resources — all built and click-tested (Section 2)
3. [ ] Real Period Manager (freeze/frozen/amendment/incorporated state machine, server-authoritative per `ARCHITECTURE.md` Section 3) — the current auto-created period is still a stopgap from session 4
4. [ ] Controls Dashboard (3 tabs) — the natural home for reserve rollup (sum(threat EMV) − sum(opportunity EMV), noted as deferred back in session 8), sensitivity analysis, and cross-module KPI summary
5. [ ] The 4 AI capabilities (Section 5) — nothing built yet; needs the Period Manager's baseline snapshots to exist first for the causally-aware narrative
6. [ ] Export Center (Excel/Power BI star-schema export, PPTX/PDF report builder)
7. [ ] File Manager
8. [ ] Real Import parsers (XER/XML first per `ARCHITECTURE.md` Section 6; MPP needs a research spike)
9. [ ] Stand up AWS production infrastructure (ECS Fargate, RDS Aurora, S3, CloudFront) — still deferred, no urgency while developing locally

No fixed next-session instruction was left at the end of session 19 — pick from the above or ask the founder.

## 4. Core Data Model (Authoritative — Carry Forward From Prototype)

This is the schema the prototype's Export Center already demonstrates as a star-schema; use it as the real Postgres schema, not just an export format.

### 4.1 Core Entities

| Table | Key Fields | Notes |
|---|---|---|
| `organisations` | id, name, plan_tier | Multi-tenancy root |
| `projects` | id, org_id (FK), name, client_name, status | |
| `periods` | id, project_id (FK), period_label, start_date, end_date, cutoff_date, freeze_status (live/frozen/incorporating), baseline_locked_flag | Drives the whole Period Manager state machine |
| `activities` | id, project_id, period_id, task_name, wbs_path, start, finish, bl_start, bl_finish, variance_days, pct_complete, total_float, is_critical | fact_schedule in prototype's export schema |
| `risks` | id, project_id, period_id, title, category, status, probability, impact, rating, emv_cost, emv_schedule_days, mitigation_status | fact_risk |
| `cost_elements` | id, project_id, period_id, element_group, description, budget, forecast, actuals, variance, cpi, spi, rev_a_baseline, cost_per_m2 | fact_cost |
| `icd_items` | id, project_id, period_id, item_type (issue/change/decision), status, priority, owner, cost_impact, schedule_impact_days, raised_date, closed_date | fact_icd |

### 4.2 Period Baselines

Each `periods` row, once frozen, must produce an **immutable snapshot** of all linked activities/risks/cost_elements/icd_items at that point in time — this is what the Baseline Comparison Engine compares against. Do not let frozen-period data be editable; this is a core trust guarantee of the product, not an implementation detail.

### 4.3 Cross-Module Linking (Critical — This Is a Genuine Differentiator)

```sql
CREATE TABLE record_links (
  id UUID PRIMARY KEY,
  source_type VARCHAR NOT NULL,  -- 'activity' | 'risk' | 'cost_element' | 'issue' | 'change' | 'decision'
  source_id UUID NOT NULL,
  target_type VARCHAR NOT NULL,
  target_id UUID NOT NULL,
  link_type VARCHAR NOT NULL,    -- 'causes' | 'impacts' | 'mitigates' | 'relates_to'
  note TEXT,
  created_by UUID REFERENCES users(id),
  created_at TIMESTAMPTZ DEFAULT now()
);
```

This single table is what powers the prototype's "Linked" tab on every record type, and the causal-chain-tracing AI narrative (Section 5.2 of the business case / Section 5 of the scope doc). Do not normalise this into separate join tables per type pair — the graph-edge shape is deliberate and was chosen specifically to support arbitrary many-to-many relationships across all 6 record types without schema changes.

## 5. AI Integration — What Must Be Rebuilt Faithfully

The prototype's AI features are not decorative; they are the product's primary differentiator per the business case. Four distinct capabilities, each with its own trust model — **do not collapse these into one generic "AI chat" feature**:

1. **Context-aware co-pilot** — system prompt is built dynamically per active module/record, enriched with the 3 most relevant causal chains from `record_links`. Always-on side panel.
2. **Causally-aware baseline narrative** — on-demand (user clicks "AI Summary"), traces `record_links` up to 4 hops from changed records since the comparison baseline, feeds traced chains + raw metric deltas into the prompt, generates a 3-paragraph root-cause narrative. **The traced chains themselves are never shown in the UI** — this was tried and removed after user testing found it cluttered; the AI's written narrative is the only surface for this reasoning.
3. **Setup Assistant** — fires once per import event, proposes new risks/cost elements/links from a fixed, ranked rule set (max 5 suggestions per trigger). **Never auto-writes** — every suggestion requires explicit Accept/Edit/Dismiss. In production this needs a real rule engine reading the actual imported file's content, not the prototype's hard-coded pattern matches.
4. **Generative Visual Assistant** — intent-matches a free-text request to one of a **fixed, whitelisted set of real data queries** against the fact tables. **This is not free-form chart generation** — there is no path for the AI to invent a number. If a query doesn't match a known intent, it says so and asks for specificity. Production should expand the intent library but preserve this constraint absolutely; it's a deliberate safety/trust design, not a limitation to "fix" by going fully generative.

## 6. Recommended Repo Structure

```
prosota-pmo/
├── PROJECT_STATE.md          <- this file, update every session
├── ARCHITECTURE.md           <- technical architecture decisions (companion doc)
├── docs/
│   ├── business-case.docx
│   ├── project-scope.docx
│   └── prototype/
│       └── prosota-pmo.html  <- reference UX spec, do not deploy this file
├── backend/
│   ├── app/
│   │   ├── models/           <- SQLAlchemy models per Section 4 schema
│   │   ├── api/               <- FastAPI routers per module
│   │   ├── services/          <- business logic, incl. causal chain traversal
│   │   └── ai/                 <- the 4 AI capability modules, kept separate
│   ├── alembic/
│   └── tests/
├── frontend/
│   ├── src/
│   │   ├── modules/           <- one folder per prototype view
│   │   ├── components/
│   │   └── lib/
│   └── tests/
└── infra/
    └── terraform/
```

## 7. Things Explicitly Out of Scope (Don't Accidentally Build These)

- BIM/IFC/4D simulation — deliberate positioning decision, platform is horizontal/sector-agnostic
- Mobile-responsive layout in Phase 1 — desktop-first per business case
- Second project tenant beyond the seed data — single real project, multi-tenant *architecture* yes, but don't over-invest in demo data variety early
- Free-form/generative chart creation — see Section 5.4 above, this is intentional

## 8. Key People & Roles

| Role | Who | Notes |
|---|---|---|
| Founder, Product, Domain Expert, Sales | Maro Sota | Makes all product/commercial decisions |
| Engineering | Claude (via Claude Code from this point forward) | Executes all code; this chat interface is no longer where engineering happens |

## 9. Session Log

| Date | What Happened | Next Session Should... |
|---|---|---|
| 2026-06-30 | Prototype completed end-to-end across an extended chat session. Business case (v3) and Project Scope document finalised. This handoff doc created to bridge into Claude Code / production build. | Set up the actual repo, copy reference docs in, and start Sprint 1 infra work per Section 3. |
| 2026-06-30 | Sprint 1 tasks 1, 2, 4 complete. Repo scaffolded, docs in place, FastAPI + all 9 SQLAlchemy models + Alembic initial migration done. API confirmed running locally at http://localhost:8000 with Postgres 16 (native Windows install). psycopg3 used as driver (asyncpg dropped — no Python 3.14 wheels). Docker Hub unreachable from dev machine — Docker Compose file exists but is not the active local dev path. | Next: decide between task 5 (Auth0) or task 6 (CI/CD), or move to first real API endpoints (Activities + Risks CRUD) to unblock frontend work. |
| 2026-06-30 | Sprint 1 tasks 4 + 6 fully complete. Full CRUD + tests for Activities, Risks, Cost Elements, ICD Items, and record_links (bidirectional graph). Cost elements: fixed/percentage dual-type with live computed values. ICD items: single-table discriminator (issue/change/decision) with type-field validation. GitHub Actions CI live — PostgreSQL 16 service container, pytest on push/PR. All pushed to GitHub (eng-lou/Prosota-PMO). Tasks 3 (AWS) and 5 (Auth0) still deferred. | Next: Auth0 integration (task 5) or AWS infra skeleton (task 3). Backend API is complete enough to unblock both. Frontend remains blocked until Auth0 exists. |
| 2026-07-01 | Auth0 JWT integration complete (task 5). Frontend scaffold live: React 18 + TypeScript + Tailwind + React Query + Vite. Auth0 SPA login working end-to-end. Project selector screen live — fetches real projects from API, creates new ones, persists selection to sessionStorage. Shell layout with sidebar nav (all 8 modules including File Manager). CORS configured. Auto-provisioning on first login (get_db_user creates org + user if not found). Projects, Periods, Users/me endpoints added to backend with tests. | Next: build the module views one at a time — Risk Register first (most core to the differentiator), then Cost Plan, then the others. Also need to run pytest after today's backend additions and commit everything. |
| 2026-07-01 | Confirmed all 71 backend tests still pass and working tree was clean at session start. Built the first real module view: **Risk Register** (`frontend/src/modules/risks/`) — full CRUD table wired to the existing `/risks` API (create/edit form, delete with confirm), plus a working demo of the `record_links` cross-linking feature (risk-to-risk links only for now, since other modules have no frontend list yet to pick targets from). Added lightweight period bootstrapping (auto-creates a "Period 1" live period if the project has none) since risks are period-scoped and Period Manager doesn't exist yet — this is a stopgap, not the real Period Manager. Wired into routing at `/risks`, replacing its placeholder. Verified: `tsc --noEmit` shows zero new type errors (5 pre-existing unrelated errors confirmed present on `main` before this change too, via `git stash`); backend and frontend dev servers both start cleanly and the new module transforms through Vite with no errors. **Not yet verified interactively in-browser past the Auth0 login wall** — that needs the founder's real login. Nothing committed yet. Also created `docs/LEARNING_LOG.md` and `docs/COMMANDS_GLOSSARY.md` (plain-English project history and command reference for the founder's own learning) — both to be kept updated every session going forward. | Next: Maro to manually click through the Risk Register in a real browser session (add/edit/delete a risk, try linking two risks) to confirm it behaves as expected, then commit. After that, build Cost Plan next, then ICD Tracker, following the same pattern established here. Real Period Manager still needed eventually — the auto-created period is a placeholder. |
| 2026-07-01 | Manual click-through surfaced a real bug: "Failed to create project" in the UI. Root cause was **not** frontend — the backend's async Postgres driver (psycopg3) can't run on Windows' default `ProactorEventLoop`, only on `SelectorEventLoop`, and plain `uvicorn app.main:app` creates its loop before `app.main` is even imported, so no fix inside the app module could arrive in time. `tests/conftest.py` already worked around this for pytest, but the live dev server never had the same fix — it went unnoticed until now because earlier smoke-testing only hit `/health` (no DB access), and this was the first real DB write attempted through a running server this session. Fixed by adding `backend/run.py`, which sets the correct event loop policy *before* calling `uvicorn.run()`. Verified with a direct DB query before asking for a UI re-test; Maro then confirmed project creation works in the real browser. Risk Register (this session's Section 9 entry above) and this fix committed together. | Next: use `python run.py` (not raw `uvicorn app.main:app`) to start the backend locally from now on — update any personal notes/shortcuts accordingly. Continue with Cost Plan module next. |
| 2026-07-01 | Decided to build all remaining module views at the same "basic CRUD" depth first (Cost Plan, ICD Tracker, then Scheduling/Dashboard/etc.), deferring prototype-fidelity polish (heat matrices, EMV summaries, print/export) until every module exists — no point polishing one screen when they all depend on each other via `record_links`. Built **Cost Plan** (`frontend/src/modules/costs/`) — full CRUD for fixed and percentage cost elements (percentage elements show their live-computed budget/forecast/actuals, matching backend behaviour). Built **ICD Tracker** (`frontend/src/modules/icd/`) — full CRUD across all three sub-types (issue/change/decision) with type-specific fields and an All/Issues/Changes/Decisions filter. **Found and fixed a real backend gap**: `icd_items` had no `title` field at all (not in the original PROJECT_STATE Section 4.1 spec either) — every issue/change/decision was unidentifiable beyond type/status/date. Added a `title` column via a new Alembic migration (`a1f3c9d2e764`), updated the schema/model and all existing tests; safe to apply as NOT NULL since the dev DB had zero `icd_items` rows. Also generalised the linking UI: extracted a shared `RecordLinks` component (`frontend/src/components/RecordLinks.tsx`) and `useActivePeriod` hook (`frontend/src/lib/usePeriod.ts`) used by all three modules, and risks/cost elements/ICD items can now genuinely link *across* module types (not just to their own type as in the original Risk Register pass) — the first real demonstration of the cross-module linking differentiator. Verified: full pytest suite (71 passed) after the migration; `tsc --noEmit` shows the same 5 pre-existing unrelated errors and zero new ones; both dev servers running, confirmed via curl/log inspection that the risk and cost-element endpoints were hit successfully by the real browser session earlier. **Cost Plan and ICD Tracker not yet clicked through by a human** — same Auth0 login wall as before. Nothing committed yet. | Next: Maro to click through Cost Plan (fixed + percentage elements) and ICD Tracker (all 3 sub-types, filter tabs, cross-linking a risk to a cost element to an ICD item) to confirm before committing. After that, continue with Scheduling/Activities and the Controls Dashboard at the same basic-CRUD depth, then do one prototype-fidelity polish pass across all modules together. |
| 2026-07-01 | Manual click-through of Cost Plan/ICD Tracker surfaced three more real issues, all now fixed. **(1) ICD create failed (500 error):** the actual cause was that the running backend process still had the pre-title version of the `IcdItem` model in memory — uvicorn's `--reload` logged "Reloading..." but the worker process never actually picked up the new column (confirmed via the raw SQL in the error log, which omitted `title` from the INSERT entirely). Fixed by fully killing and restarting the process rather than trusting `--reload`; confirmed the fresh process has `title` mapped before asking for a re-test. **(2) CPI/SPI were exposed as free manual-entry fields on Cost Plan**, but these are calculated metrics, not user input — Maro caught this. Removed them from the create/edit form entirely (values stay read-only "—" in the table until real computation logic exists, which needs schedule/progress data from the not-yet-built Scheduling module for SPI at least). **(3) ICD status was free text instead of a dropdown** — fixed with a per-item-type status list (`STATUSES_BY_TYPE` in `frontend/src/modules/icd/types.ts`) matching the prototype's vocabulary (Issue: Open/In Progress/Overdue/Closed; Change: Pending Approval/Approved/Rejected/Implemented; Decision: Pending/Decided). **Also added human-readable reference codes** (e.g. `RSK-0001`, `CST-0001`, `ISS-0001`/`CHA-0001`/`DEC-0001`) across Risks, Cost Elements, and ICD items — Maro flagged their absence as a real gap versus the prototype. Implemented as a `code` column (unique per project, per type, 4-digit zero-padded, never reused even after deletes — chosen deliberately over reusing counts, to survive at volume) via a new migration (`b7e2a4f81c33`) with backfill for the one pre-existing risk and cost element row, a shared `next_code()` generator (`app/services/reference_codes.py`), and wiring through all three services/schemas/frontend modules (table columns + `RecordLinks` candidate labels now show `CODE: title`). Verified: full pytest suite (71 passed, after fixing 3 tests that constructed `Risk()` directly without a code); `tsc --noEmit` unchanged (same 5 pre-existing errors, zero new); backend fully restarted from a clean process this time. **Not yet re-confirmed by a human** — same Auth0 wall. Nothing committed yet (this entry, the previous Cost Plan/ICD entry, and the Risk Register generalisation are all still uncommitted together). | Next: Maro to retest ICD creation, the status dropdown, Cost Plan without CPI/SPI inputs, and confirm the RSK-/CST-/ISS-/CHA-/DEC- codes show up correctly, then commit everything from this session in one go. |
| 2026-07-01 | Maro (PM domain expert) flagged that EMV was calculated wrong, and pointed to three PMBOK/PMP reference PDFs now in `docs/` (`PMBOKGuideSeventh.pdf`, Rita Mulcahy's PMP Exam Prep 11th Ed., ProcessGroups Practice Guide) — added a standing rule ([[feedback_pm_domain_accuracy]] in memory) to consult these before implementing any PM-domain calculation going forward, rather than guessing. Root cause confirmed via a research subagent and then by reading Chapter 12 ("Risks and Issues") of Rita Mulcahy directly, at Maro's request, rather than relying only on the subagent's summary: **EMV = Probability x Impact, where Impact must be a real monetary/duration value — never a normalized 0-1 score.** The bug: `risks.impact` was a qualitative 0-1 heat-map severity score (correct for the probability-impact matrix), but `emv_cost`/`emv_schedule_days` were exposed as free manual-entry fields holding the *final* EMV figure directly, with no calculation happening at all (the same class of mistake as the CPI/SPI issue). Fixed via migration `c92d4e6f0a17`: added `cost_impact` (£) and `schedule_impact_days` (days) as the real quantitative inputs ("impact if this risk occurs"), widened `emv_schedule_days` to numeric (expected values are fractional, e.g. 0.65 x 20 = 13.0 days, not an integer), and made `rating`/`emv_cost`/`emv_schedule_days` computed-only in the service layer (`_apply_computed_fields` in `app/services/risk.py`) — never accepted as API input. Recomputed the pre-existing demo risk rows so stale manually-entered values (one row had emv_cost=£5,000,000 from earlier manual testing) don't linger. Added 4 new backend tests locking in the exact PMBOK example (0.65 x £40,000 = £26,000) and the rating/EMV independence. Frontend (`RiskForm.tsx`/`RiskRegister.tsx`) updated to match: cost/schedule impact are the real inputs, rating/EMV shown read-only. Verified: 75 tests passing (71 + 4 new); `tsc --noEmit` unchanged; both servers restarted clean. **Noted for later, not acted on now:** Rita Mulcahy's chapter also covers threat-vs-opportunity sign convention (opportunities reduce contingency reserve, threats increase it — reserve = sum(threat EMVs) − sum(opportunity EMVs)) and aggregate reserve calculation, neither of which exist in the current Risk model (no threat/opportunity distinction, no reserve rollup) — relevant when the Controls Dashboard gets built. Not yet re-confirmed by Maro in browser; nothing committed yet. | Next: Maro to retest Risk Register (probability/impact for heat-map rating, cost/schedule impact for EMV, confirm 0.65 x £40,000 = £26,000-style math looks right), then commit everything from this session together. Consider whether threat/opportunity + reserve aggregation should be a near-term follow-up or wait for the Controls Dashboard. |
| 2026-07-01 | Maro pushed back: fix EMV specifically wasn't enough — asked for the full Chapter 12 methodology (threats/opportunities, watch lists, sensitivity analysis, etc.) to be reconciled against what was already planned for Risk Register in the prototype/scope docs, before continuing to any other module. Created **`docs/RISK_MODULE_PLAN.md`**: read the prototype's actual Risk Register markup in full (6-tab detail panel — General, Criteria & Thresholds, Qualitative Analysis, Quantitative Analysis, Mitigation & Response, Linked) plus the scope doc/business case sections on Risk Register, and reconciled all of it against Ch. 12. Produced a gap-analysis table and a 6-phase proposed plan (risk statement/ownership quality → threat/opportunity split → inherent/residual rating → mitigation actions sub-list → configurable criteria & thresholds → 3-point quantitative estimates), with reserve rollup/decision-tree/sensitivity-analysis explicitly deferred to the future Controls Dashboard rather than bolted into the register. Notable finding: the prototype's own Mitigation & Response tab mixes threat and opportunity response strategies in one dropdown, which is itself non-PMBOK-compliant — flagged as something to fix, not copy. Nothing implemented yet — this is a planning doc awaiting Maro's direction on phase ordering and a few open questions (org-level vs project-level criteria config; single-point vs 3-point EMV formula). | Next: Maro to review `docs/RISK_MODULE_PLAN.md` and confirm phase order/answer open questions, then implement Phase 1 onward before resuming Cost Plan/ICD Tracker/other modules. |
| 2026-07-01 | Maro said "go ahead" — implemented all 6 phases from `docs/RISK_MODULE_PLAN.md` in one session, tracked via TaskCreate/TaskUpdate. **Phase 1:** risk statement fields (`cause`/`effect`/`rationale`/`risk_owner`) + `area` (second RBS dimension alongside `category`). **Phase 2:** `risk_type` (threat/opportunity) + type-validated `response_strategy`; discovered and implemented an **asymmetric EMV sign convention** per the book's own worked examples — a threat's cost EMV is negative but its schedule EMV is positive (added days), and opportunities are the exact mirror — locked in with dedicated tests both ways. **Phase 3:** residual (post-mitigation target) probability/impact/rating alongside the existing inherent triple, plus a real 5×5 heat-matrix component (`frontend/src/components/HeatMatrix.tsx`) shown twice per risk. **Phase 4:** new `risk_mitigation_actions` table with its own `MA-01`-style codes sequential **per risk** (a dedicated generator — the shared per-project `next_code()` helper didn't fit), full nested CRUD, plus `contingency_plan`/`fallback_plan` fields. **Phase 5:** configurable project-level Criteria & Thresholds (two dedicated tables, since impact needs both a cost and a schedule range per level while probability doesn't), auto-seeded with the prototype's own default bands on first access, collapsible inline-editable panel at the top of Risk Register. **Phase 6:** 3-point (Min/Most Likely/Max) estimates — renamed `cost_impact`/`schedule_impact_days` to `cost_most_likely`/`schedule_most_likely_days` (safe, nothing using the old names was committed) and added min/max as range context; EMV deliberately still uses only the most-likely value since the reference material's worked examples are single-point and a PERT-weighted formula wouldn't be verifiable against source. Six migrations total (`d4a1e8b3c521` → `c1e7a93f5d84`). Verified throughout: 97 backend tests passing (up from 71 at session start — 26 new tests across 3 new test files plus additions to `test_risks.py`), `tsc --noEmit` unchanged (same 5 pre-existing unrelated errors) after every phase, backend fully restarted clean after every migration, existing demo risk rows re-verified intact after the Phase 6 rename. **Not yet confirmed by a human in the browser.** Nothing from this session (Cost Plan, ICD Tracker, EMV fix, or all 6 Risk Module phases) has been committed yet. | Next: Maro to do a full click-through of the Risk Register (all 6 phases — statement fields, threat/opportunity + response strategy, inherent/residual heat matrix, mitigation actions, criteria & thresholds panel, 3-point EMV) to confirm before committing. After that: Cost Plan, ICD Tracker, then Scheduling/Dashboard, at the basic-CRUD depth established earlier — the Risk Module is now the fidelity bar the others were deliberately not held to yet. |
| 2026-07-01 | First real click-through of the new Risk Module hit "failed to create risk" (500 error) — found via backend log: `mitigation_status` was still `varchar(50)` from before this session's phases, and Maro (realistically) typed a full narrative paragraph into it, exceeding the limit. Root cause: `mitigation_status` is conceptually a narrative field like `contingency_plan`/`fallback_plan`/`rating_narrative` (all `Text`, unlimited), not a short status code — it was just never revisited when those were added in Phase 4. Fixed via migration `d5f8b2a047e6` (widen to `Text`), added a regression test using the exact paragraph that broke it, and changed the frontend input from a single-line `<input>` to a `<textarea>` to match its actual purpose. 98 tests passing (was 97). Backend fully restarted clean. | Next: Maro to retry creating that same risk now, then continue the full click-through. |
| 2026-07-01 | Maro confirmed the Risk Module "works extremely well" — committed everything from this session (Cost Plan, ICD Tracker, EMV fix, all 6 Risk Module phases, mitigation_status fix) as one commit. Gitignored the 3 PMBOK/PMP reference PDFs first (~108MB total, copyrighted commercial material — Maro chose gitignore over committing them). Then Maro flagged the Risk Module still wasn't fully done: missing `date_raised` (Date Identified, matches prototype), `date_closed`, an "impact date" (interpreted as `expected_impact_date` — when the risk is expected to occur/materialise, per Ch.12's risk factor "expected timing for it to occur"; flagged this interpretation for Maro to correct if wrong), and `last_reviewed_date` (matches prototype). Also asked for a second mechanism beyond `rating_narrative` (which stays as-is): a dated log of "what changed and why" whenever probability/impact/status change. Implemented as a new **`risk_reassessments`** table — append-only, timestamped, distinct from the single-point-in-time `rating_narrative` — with `RiskForm` auto-detecting when a trigger field (probability/impact/probability_residual/impact_residual/status) differs from the risk's saved value and surfacing an optional "what changed and why?" prompt; if filled in, a reassessment entry is logged alongside the update. Logging a reassessment also auto-bumps `last_reviewed_date`. A manual "+ Log a review" button covers the case where a review happened but nothing changed. New migration `e2a6c81b9f34`. 105 tests passing (was 98 — 7 new). `tsc --noEmit` unchanged. Backend restarted clean. **Not yet confirmed by Maro — this addition is not committed.** | Next: Maro to confirm the "expected impact date" naming/meaning is right, test the 4 new date fields and the reassessment prompt/log (change probability, see the prompt appear; log a manual review; confirm last_reviewed_date updates), then commit this addition on top of the already-committed Risk Module work. |
| 2026-07-01 | Maro called the reassessment/date work "brilliant" and asked for two more things: (1) reassessment log entries should be editable/deletable, not append-only as originally designed; (2) the prototype's toolbar utility features — Search, Filters, Group, plus real Import/Export and Print/Preview. Clarified two scope questions first: the prototype's "C-3" button has no onclick handler anywhere in its source — confirmed decorative, skipped; and Import (real XER/MPP/XLSX parsing + field-mapping UI) was descoped to a separate follow-up, since building a fake import UI would repeat the exact mistake this whole session has been fixing — Export and Print were built for real instead. Delivered: **(1)** PATCH/DELETE endpoints for `risk_reassessments` (was create/list-only) plus inline edit/delete UI. **(2)** Client-side Search (title/code/theme/area/owner), a Filters panel (status/risk type/theme/area, all multi-select where sensible), and Group-by (theme/area/status/risk type) — all operating on the already-loaded risk list, no new endpoints needed. **(3)** Real CSV export (`exportRisks.ts`) of the currently visible/filtered risks — opens directly in Excel; deliberately did not add a fake `.xlsx`/`.xml` button, since CSV is genuinely functional today without a new dependency. **(4)** Real Print/Preview using the browser's native print, not a mockup: a `.print-only`/`.no-print` CSS convention added globally (`index.css`, `Sidebar.tsx`), a checkbox column for selecting specific risks, and a dedicated `RiskPrintView` component with two modes — "print as shown" (the current filtered/grouped table) or "print selected, full detail" (a per-risk report: statement, both heat-map assessments, 3-point cost/schedule + EMV, response strategy, contingency/fallback — everything stored on the risk record itself; mitigation actions/reassessment history are not embedded since those need a separate fetch per risk, noted as a scope limit rather than silently omitted). 108 backend tests passing (was 105 — 3 new for reassessment edit/delete). `tsc --noEmit` unchanged throughout (same 5 pre-existing errors). Backend restarted clean. **Not yet confirmed by Maro — none of this is committed.** | Next: Maro to test reassessment edit/delete, search/filters/group, the Export CSV button, and both Print modes (list + full-detail), then commit. Import (real file parsing) remains a deliberately separate future task. |
| 2026-07-01 | Committed and pushed the Risk Module date/reassessment/toolbar work (`e8968a0`) after Maro confirmed it worked ("brilliant work" / "good implementation, good speed, hardly any errors aside from domain knowledge which i provide" — saved as [[feedback_workflow_validated]] memory). Maro then asked to apply the same rigorous process to ICD Tracker next: full gap analysis + plan doc before implementing, per the pattern established for Risk. Created **`docs/ICD_MODULE_PLAN.md`**: read the prototype's full ICD Tracker markup (6 KPI cards, 3 type-specific sub-tabs, a 6-tab type-adaptive detail panel — General/Impact & Links/Actions & Comments/History & Audit/Notifications/Linked) and the scope doc section, then read Rita Mulcahy Ch. 4 "Perform Integrated Change Control" in full plus the Issue Log artifact (Ch. 6, Figure 6.7) directly. Found concrete gaps against the book's own Issue Log structure (`raised_by`, `resolution_due_date`, `resolution` — all explicitly named, all missing from our schema) and against Integrated Change Control theory (change impact must be assessed across scope/quality/risk/cost/schedule, not just cost/schedule; changes need a CCB-style approval decision distinct from generic status). Produced a 6-phase plan (Issue Log field gaps → change-specific fields incl. Quality Impact → decision "if late" field → Action Items sub-list, reusing the Risk Mitigation Actions pattern → Comments, newly buildable since real Auth0 user identity exists → KPI strip + generalising Risk's Search/Filters/Group/Export/Print components). Deferred, with reasoning: automatic History & Audit trail (Risk's reassessment log is user-prompted, not automatic — flagged as a real architectural fork, not assumed either way) and Notifications (no real email/in-app infrastructure exists anywhere in the app yet — same "don't fake it" reasoning as Import). Nothing implemented yet — plan doc awaiting Maro's answers to 4 open questions before Phase 1 starts. | Next: Maro to review `docs/ICD_MODULE_PLAN.md`, especially the History & Audit automation question (biggest fork) and the change-type category names, then implement Phase 1 onward the same way the Risk Module phases were done (TaskCreate-tracked, verified increment by increment). |
| 2026-07-01 | Maro approved the full `docs/ICD_MODULE_PLAN.md` ("reuse Risk's user-prompted pattern, then proceed. i approve of the analysis and steps"), authorizing all 7 phases. Implemented sequentially, TaskCreate-tracked, verified after every phase: **Phase 1** Issue Log field gaps (`description`/`raised_by`/`due_date`/`resolution`, migration `f4b8e2c917a5`). **Phase 2** Change-specific Integrated Change Control fields — `change_type` (variation/client_instruction/omission), `ccb_decision` (approved/rejected/deferred, deliberately kept distinct from the generic `status` field per the plan), `rejection_reason` (validated as only settable once `ccb_decision == 'rejected'`), `contract_reference`, `cost_claim`, `eot_claim_days`, `quality_impact` (migration `ce2b0dd1ef9f`). Maro then requested a batch of fixes/additions on top: **(a)** fixed a duplicate "Open" status option in the issue dropdown, caused by a casing mismatch between the option list (Title Case) and the actually-stored value (lowercase) — standardised every status to lowercase with a separate display-label map, and fixed each item type's create-default to match its own valid status set (previously all three types defaulted to `"open"` even though only Issue has that as a valid option). **(b)** Priority changed from free-text to a dropdown (Critical/High/Medium/Low). **(c)** `due_date` and `required_by` were redundant for Decisions — hid the generic Due date field for that type, and repointed the tracker's "Due" column + Impact-column display to use `required_by`/`decision_maker` instead. **(d)** Built **ICD Criteria & Thresholds** (`icd_criteria` table, migration `900951394751`) — narrative definitions of what each Priority/Severity/Quality-Impact level means, project-scoped and lazily-seeded with PM-practice defaults, mirroring Risk's Criteria & Thresholds panel but simpler (ordinal levels only, no numeric ranges, since these three dimensions are categorical). **Phase 7** (done ahead of 3–5, per Maro's request order) last_reviewed_date + editable/deletable reassessment log (`icd_reassessments`, migration `d601e4a2b7a6`), reusing Risk's exact trigger-field-detection pattern (status/priority/severity/ccb_decision/cost_impact/quality_impact). **Phase 6** utilities toolbar — search/filters(status,priority,owner)/group-by/CSV export/print(list-or-full-detail-selected), generalised from Risk's `RiskRegister.tsx` toolbar into `exportIcdItems.ts` + `IcdPrintView.tsx`. Maro confirmed this batch worked and it was committed/pushed, then said "carry on" to continue the remaining phases: **Phase 3** Decision `if_late_consequence` field (migration `63df6368364e`). **Phase 4** Action Items sub-list (`icd_action_items`, migration `d6e2149d0cc5`) — exact reuse of Risk's `risk_mitigation_actions` pattern (own `ACT-01`-style codes sequential per item). **Phase 5** real Comments (`icd_comments`, migration `b1094c80580b`) — author is the actual authenticated user via the existing `get_db_user`/Auth0 flow (captured as `author_id` + a point-in-time `author_name` snapshot), editable/deletable only by the original author, enforced server-side (403 otherwise) not just hidden in the UI. Also closed a gap in the already-committed Phase 6 batch: added the **KPI strip** (`IcdKpiStrip.tsx`) that the approved plan specified but hadn't actually been built yet — Open/Overdue Issues, Pending/Approved Changes + net cost/schedule impact, Pending/Made Decisions, pure client-side aggregation over the loaded item list. Maro then caught a real bug testing it: changing a Change's Status from Pending to Approved didn't move the KPI numbers at all. Root cause: the KPI's pending/approved counts only checked the separate `ccb_decision` field, not the more prominent `status` field Maro actually edited (the two fields overlap in practice since `status` already offers `pending_approval`/`approved`/`rejected` options) — fixed so a change counts as approved/rejected if *either* field says so. 147 backend tests passing across the whole ICD session (was 108 at the start of this stretch — 5 new migrations, 6 new test files); `tsc --noEmit` unchanged throughout (same 5 pre-existing unrelated errors, zero new). Maro confirmed the KPI fix and the whole ICD Tracker works in the real browser — ready to commit. | Next: commit and push this session's ICD Tracker work. Cost Plan still needs to be brought up to the same Risk/ICD rigor level (statement quality, criteria/thresholds, toolbar, reassessment log) — likely the next module to revisit. Real Import (file parsing) and Notifications remain deliberately deferred, no infrastructure yet. |
| 2026-07-01/02 | Brought Cost Plan up to the same rigor as Risk/ICD. Wrote `docs/COST_PLAN_MODULE_PLAN.md` first; Maro answered 4 open questions (split manual Status from a computed variance band; EAC = BAC/CPI as the single formula; generalise the Risk/ICD reassessment-log pattern now rather than copy a 3rd time; GFA/space-count as an optional project-level field). Implemented Phases 1-7 in one continuous run under a blanket "proceed without stopping" authorization (Maro clarified partway through: implement every phase, don't skip any): **Phase 1** general field gaps (`cost_owner`/`status`/`scope_note`/`variance_commentary`/`qs_signoff_name`/`qs_signoff_date`, migration `3fd361dbc813`). **Phase 2** EVM fields — `pct_complete`, dropped `cpi`/`spi`/`variance`/`cost_per_m2` as stored columns (now always computed), added `projects.gfa_m2`/`space_count` (migration `a4404274658f`). Mid-Phase-2, Maro raised a genuine fork — build Scheduling first, or keep going with %-complete-based EV now? — resolved via PMBOK Ch.9 (physical % complete is a valid EV technique without a network schedule); Maro chose to continue. **Phase 3** Cost Variance Criteria & Thresholds (`cost_variance_criteria`, migration `61a49f3dfc9e`), mirroring Risk/ICD's pattern. **Phase 4** Cost Rate Lines (`cost_rate_lines`, migration `5a42b3da6412`) for percentage-element rate breakdowns. **Phase 5** Cost Commitments (`cost_commitments`, migration `e2fde8c5a226`). **Phase 7** generalised the Risk/ICD reassessment-log pattern into one shared polymorphic `reassessments` table (`record_type`/`record_id`, no per-parent FK columns, migration `cc7d1cd00847` — migrates existing `risk_reassessments`/`icd_reassessments` data across via raw SQL, drops the old tables, full `downgrade()` restoring both), a single shared `ReassessmentLog.tsx` component replacing the two per-module copies, and a shared `last_reviewed_date` (migration `fad42d2dc977`). Maro then click-tested the real thing and caught five real bugs in sequence, each fixed immediately: **(1)** the in-product EVM tab cited "Rita Mulcahy Ch. 9" by name — Maro called this "very unprofessional"; removed, standing principle added (consult reference material internally, never name it in user-facing UI). **(2)** SPI was mathematically broken — with no time-phased schedule PV was set equal to BAC, which makes SPI collapse to exactly `pct_complete/100`, a fake duplicate number; Maro said to remove it (not patch it) and do only minimal EVM fixes, deferring real SPI/SV until Scheduling exists — removed SV and SPI entirely from schema/service/frontend (types, table, print view, CSV export). **(3)** the `forecast` field was a manual input duplicating the computed EAC — removed from Create/Update schema, now always computed server-side (EAC once progress is assessed, falls back to budget otherwise), which required rewriting percentage-element subtotal aggregation from a SQL-level sum to a per-row Python computation. **(4)** `rev_a_baseline` duplicated `budget` — now auto-set once from `budget` at creation (fixed elements only) and frozen thereafter (removed from the Update schema entirely so routine budget edits can't silently erase it). **(5)** the per-row Variance Band badge and the Cost Summary panel contradicted each other on a real test row — root cause was the badge computing its % from `variance` (budget vs. the now-frozen baseline, ~0 unless deliberately rebaselined) while the Summary compared budget vs. forecast; unified both onto the same forecast-vs-budget formula (new `elementForecastVariance()` helper), replaced the Summary's "vs Rev A Baseline" section with "Budget vs Forecast", made Variance Threshold descriptions editable, fixed a stale "Rev A baseline" reference in `CostPrintView.tsx`'s detail mode, and updated+backfilled the default threshold wording (`_DEFAULT_CRITERIA`) via a live-DB verification script. 171 backend tests passing (was 147 at session start). `tsc --noEmit` unchanged (same 5 pre-existing unrelated errors, zero new). Backend restarted clean after every migration. Maro confirmed the fixes and said to commit and end the session. | Next: Scheduling module, per Maro's explicit "we'll carry on with schedule tomorrow." Cost Plan is now at full Risk/ICD rigor parity. Real SPI/SV can only be built once Scheduling provides a time-phased PV baseline — revisit then. |
| 2026-07-02 | (Session 12) Built the entire Scheduling module — all 9 phases of `docs/SCHEDULING_MODULE_PLAN.md`, the biggest single-session build so far. Wrote the plan doc first (prototype + PMBOK7/Rita Mulcahy Ch.8 gap analysis, `bardsoftware/ganttproject` confirmed as clean-room algorithmic reference only — GPL-3.0/Java, no code reuse); Maro confirmed a real WBS hierarchy (MS Project style, not P6's separate WBS-dictionary entity), a real CPM engine now ("very important to get it right"), multi-calendar support with per-activity override, DCMA checks 1-12 as priority, and a staged rollout (later relaxed to "carry on" after Phase 1). **Phase 1** field gaps + fixed the same computed-field bug already caught 5+ times (`total_float`/`is_critical` stop being manual input). **Phase 2** self-referencing `parent_id` WBS hierarchy with auto-promote-to-summary-on-indent, computed `wbs_path`, delete-with-choice (cascade vs. promote children — added mid-session per Maro's explicit request). **Phase 3** `activity_relationships` (FS/SS/FF/SF + signed lag) and constraints (asap/snet/ms/fnlt); Gantt gained dependency arrows. **Phase 4** multi-calendar (`calendars`/`calendar_exceptions`, one project default + per-activity override, per Maro's Saturday-working-calendar example). **Phase 5 — the big one**: `app/services/scheduling_cpm.py`, a real forward/backward pass (calendar-aware working-day arithmetic, all 4 relationship types, all 4 constraint types, full multi-hop cycle detection, total/free float, `is_critical`). This is the breaking change the whole module built toward: `start`/`finish` stopped being manual input and became genuinely computed — same discipline as every other computed field in this codebase, just the biggest one yet. **Phase 6** repeatable "Set Baseline" action (deliberately not a one-shot freeze like Cost's `rev_a_baseline` — a schedule baseline gets re-captured on every formally agreed revision). **Phase 7** DCMA 14-Point checks 1-12 (a non-PMBOK standard — numbering matches the prototype's own 8 example rows where shown, extended for the rest, flagged for Maro's correction since it's outside our reference material). **Phase 8** reassessment log (Activity registered into the shared polymorphic pattern) + toolbar (search/filters/export/print — deliberately no Group-by, since the WBS hierarchy already provides grouping and a second layer would break the Gantt's row-index alignment). **Phase 9** reschedule tool (shifts the period anchor + recomputes — the only honest lever once dates are fully computed; hard-constrained activities correctly don't move). Found and fixed two real SQLAlchemy footguns along the way, both now documented in code comments: `db.expire_all()` after a DB-level cascade delete leaves *other* session objects (like `ActivityRelationship`, which holds FK columns) merely expired rather than removed, and an unrelated later query's autoflush pass touching one mid-flight raises `MissingGreenlet`, since the required nested DB round-trip can't run inside another one — fixed with explicit ORM deletes of affected rows first, plus `expunge_all()` (detach, don't invalidate) instead of `expire_all()`; and a real ordering bug where WBS rollups (`_recompute_hierarchy`) must run *after* CPM sets descendant dates, not before, or a relationship change that shifts a child's dates leaves the parent's rolled-up dates stale. 242 backend tests passing (was 171 at session start — every phase added its own dedicated test file). `tsc --noEmit` unchanged throughout (same 5 pre-existing unrelated errors, zero new). Backend restarted clean after every migration (and once mid-session after discovering `--reload` had silently missed a new route — confirmed via the live OpenAPI schema, not assumed). Session survived several unexplained process interruptions (dev servers and even the Claude Code session itself repeatedly restarting mid-work, cause unknown — likely local machine/terminal, not this harness) by resuming cleanly from git state each time. **Not yet clicked through end-to-end by Maro in the browser.** | Next: Maro to click-test the whole Scheduling module (add activities, indent into a WBS, link with all 4 relationship types, add calendars, watch the critical path light up red, set a baseline, run the DCMA quality check with his own threshold corrections if the numbering's off, log a reassessment, try the reschedule tool) before considering it done. Real Import (file parsing) and a Resources module remain deliberately deferred, same reasoning as every other module. Once confirmed, Cost Plan's real SPI/SV can finally be built — Scheduling now provides the time-phased baseline it was waiting on. |
| 2026-07-02 | (Session 13) Small spreadsheet-style backlog for the Scheduling activity table: Show/Hide Columns (remembered via localStorage), inline double-click editing (name/duration/%complete/type), and per-row copy/paste of reusable settings. A fourth item — an hour-level calendar editor — turned out to need a real decision: purely cosmetic, or should the CPM engine itself start working in hours? Maro chose the real version ("Full hour-level CPM scheduling"), so it was broken out into its own larger task rather than rushed in. 242 tests unchanged (no backend touched this session), `tsc`/build clean. Not yet clicked through. | Next: the big hour-precision rebuild (calendar data model → CPM engine → Gantt), tracked separately. |
| 2026-07-02 | (Session 14) The big one: rebuilt calendars and the CPM engine from whole-day to hour precision. Calendars now have a real daily shape (start/end time + repeating breaks like lunch, with net hours/day computed, never typed); non-working exceptions can target part of a day; duration/dates/float are hours-based throughout; the CPM forward/backward pass walks working minutes, correctly handling partial-day gaps (the old "must start the day after, never same day" rule was dropped as an artifact of day-granularity, not a real constraint). A genuine edge case was found and documented: a deadline landing exactly at Friday close-of-business can show zero float instead of negative, because there's no working time between it and Monday morning to measure a distance in — not a bug, a property of hour-precision math. Existing data was converted (days→hours, reversibly). 250 tests (was 242). Not yet clicked through. | Next: Maro to click-test the hour-precision rebuild and the session 13 spreadsheet features together. |
| 2026-07-02 | (Session 15) Maro tested session 14 and found a real double-click bug (single-click's "open popup" action fired before double-click's "rename" action could cancel it — fixed with the standard delayed-single-click trick). Corrected a design assumption: editing Start via double-click should set a *soft* constraint (matching real P6/MS Project behaviour), not hard; editing Finish recalculates duration instead. Added Free Float, Baseline Start/Finish columns, and clarified "Var (d)" → "Fin. Var (d)". Then the big addition: a whole **Resources module** (Labour/Equipment/Material/Subcontractor), fully integrated with Cost Plan (resource assignments become the real source of an activity's cost, with an explicit one-way "unlink" if a human overrides the figure by hand in Cost Plan) — and a genuine SPI in Cost Plan finally became possible (Planned Value spread across each activity's baselined window). 276 tests (was 250). Not yet clicked through. | Next: Maro to click-test the whole Resources module and the Cost Plan SPI figure. |
| 2026-07-02 | (Session 16) Maro tested Resources and found it had no way to actually assign a resource to an activity from the UI — fixed with two entry points. Costing model corrected: Labour/Equipment now price as day-rate × utilisation% (follows duration automatically) rather than an ill-fitting quantity×rate; Materials keep qty×rate, Subcontractors stay lump-sum. The separate "Resources popup" and the activity's edit popup were consolidated into one unified detail panel (old floating form removed entirely), later split horizontally and de-cluttered (Commentary box removed as duplicating the reassessment log) after two rounds of "too tall" feedback. Gantt bars/gridlines/today-marker got a visual pass; table/pane dividers became drag-to-resize (`<colgroup>` fix for a resize bug). Print was fixed twice: a blank-page bug (scrolling panes don't paginate) fixed with a dedicated print-flow view, then the Gantt was added to that same print page with a shrink-to-fit step computed at print time. 281 tests (was 276). **Maro clicked through and confirmed this session's work — committed and pushed.** | Next: continue polishing Scheduling per any further real-use feedback, or move to Controls Dashboard. |
| 2026-07-03 | (Session 17) Added the full EVM column set (AC/PV/EV/CV/SV/CPI/SPI/BAC/EAC/ETC) to Scheduling itself, reusing Cost Plan's exact formulas. Three corrections from Maro's real P6 experience, each a genuine domain-accuracy catch: (1) % Complete was a duplicate editable field in both Scheduling and Cost Plan, causing numbers to disagree — made Activity the single source of truth, synced one-way into Cost Plan (now read-only there); (2) Planned Value was wrongly gated on a captured baseline and prorated against frozen baseline dates — corrected to use the activity's own live dates (baseline measures *variance*, PV measures "how much should be done by now against today's plan" — a different question); (3) the "data date" PV used was hardcoded to today's real calendar date instead of the project's actual reschedule anchor (`period.start_date`) — fixed via one shared `data_date_for_period` function used everywhere. That same test also surfaced a correct instinct from Maro: an activity with real progress shouldn't have its Start moved by a reschedule at all (progress makes Start a recorded fact, not a forecast) — required a real CPM engine change, verified against the full CPM+reschedule test suite before acceping. Remaining Duration became computed (not typed) from Duration × (100%−%Complete), matching the "never expose a derivable value as manual input" rule. Reschedule gained an exact-date picker alongside shift-by-amount. 288 tests (was 281). **Maro tested each fix live in the browser as it landed — committed and pushed.** | Next: Controls Dashboard, or continue Scheduling refinements as real use surfaces them. |
| 2026-07-03/04 | (Session 18) Two halves. **First, a false start:** replaced Scheduling's homemade Gantt/grid with Syncfusion (paid third-party library) at Maro's request; fought rendering bugs (blank-screen crashes, layout breakage) for several rounds until Maro called it — "it seems this was a bad idea... purge all syncfusion related revisions" — reverted cleanly, keeping the few genuinely good fixes made in the same window. Lesson: a mature third-party library isn't automatically less work, especially when not designed for this app's exact rendering pattern. **Second, a long real feature-polish stretch:** named/saved baselines (replacing the old single-overwriting "Set Baseline"); bulk row actions via checkboxes (duplicate, assign-common predecessor/successor/calendar/resource); a shared editable print letterhead across all 4 modules; named/saved visual Layouts (Gantt colours, WBS-level shading, fonts); Gantt visual overhaul (bracket-shaped WBS summary rows, proper milestone/baseline colours, Day/Week/Month/Quarter/Year zoom); DCMA checks made editable per project with their own saved reports/print/CSV export; a print-view bug fixed so it always matches on-screen column visibility. Then two real bugs, investigated properly rather than guessed at: Duration % Complete showing 100% on a same-day activity (date-only comparison instead of full timestamp — fixed everywhere it's used); and activities appearing to "vanish" after reschedule (investigated by replaying Maro's exact steps against the real DB — no data loss; two unrelated activities landing on identical dates just visually overlapped on the Gantt). Added a real time-of-day on the data date and real Reschedule error messages (previously failed silently, requiring two attempts). 321 tests (was 288). **Maro tested and confirmed the whole module — committed and pushed**, except the very last addition (WBS collapse/expand), confirmed working the next morning along with a new multi-row indent/outdent fix. | Next: Controls Dashboard, or continue Scheduling refinements as real use surfaces them. |
| 2026-07-04 | (Session 19) Project Selector couldn't actually delete old projects — investigation found the existing DELETE button would have thrown a database error on any project with real data, since ~15 dependent tables weren't wired to cascade. Maro chose to build both a safe reversible Archive **and** fix permanent Delete properly, rather than picking one. Added cascading cleanup across every project-dependent table (periods, activities, calendars, resources, risks + mitigation actions, cost elements + commitments/rate lines, ICD items + action items/comments, criteria tables, saved baselines); fixed a subtle ORM bug where the app was trying to null out a project FK before the DB's own cascade rules could run (must issue a direct delete instead); kept the existing "can't delete an in-use resource" safety net intact by explicitly clearing assignments first during a project delete only. Project Selector now has a checkbox per project plus a bulk action bar (Set Active / Archive / Delete-with-confirmation). 322 tests (was 321 — added a real cross-module cascade-delete regression test). **Committed and pushed at Maro's request.** | Next: no explicit next step left — Controls Dashboard, real Period Manager, or AI capabilities are the largest remaining unbuilt pieces (see Section 3). |

---

**If you are Claude reading this at the start of a new Claude Code session:** read this file, then `ARCHITECTURE.md`, then check `git log` for what's actually been committed (this file may lag behind reality if a session forgot to update it — trust the commit history over stale prose). Update Section 9 before you finish your session, even if the session was short.
