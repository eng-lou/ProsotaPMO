fastapi==0.111.1
uvicorn[standard]==0.30.1
sqlalchemy[asyncio]>=2.0.36
psycopg[binary,pool]==3.2.10
alembic>=1.13.2
pydantic-settings==2.3.4
python-dotenv==1.0.1
PyJWT[crypto]>=2.8.0
pye57==0.4.19
# Cloud2BIM (app/services/cloud2bim/, vendored from
# https://github.com/VaclavNezerka/Cloud2BIM, MIT) — "Generate IFC" from a
# Site Capture's point cloud. open3d deliberately excluded (see that
# directory's aux_functions.py's own header): it's only ever touched by
# optional-visualization code paths this app always disables, and its own
# PyPI wheels don't support this app's Python version anyway.
#
# ifcopenshell/opencv-python/scikit-image/pandas/matplotlib deliberately
# NOT installed here (2026-08-23, first Vercel deploy) — Vercel's Python
# function bundle has a hard 500MB cap, and this app's own build hit
# 826MB with them in; these five are used *only* inside
# app/services/cloud2bim/ (grepped every import across the whole backend
# to confirm — nothing else touches them), and cloud2bim_convert.py
# already runs that whole pipeline as an isolated subprocess specifically
# so a crash in it can never take down the real server (see that file's
# own header) — site_capture.py already catches Cloud2BimError around
# every call site, so a missing-dependency failure here degrades to a
# normal error response, not an outage. Re-add all five (opencv as
# opencv-python-headless instead — same swap, still no GUI calls anywhere
# in aux_functions.py, confirmed) if/when Cloud2BIM moves off pause.
PyYAML==6.0.3
tqdm==4.70.0
